name: hatsune
description: "Hatsune Assistant — Coding assistant con avatar 3D Hatsune Miku."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  model_viewer_plus: ^1.10.0
  google_fonts: ^6.2.1
  http: ^1.2.1
  audioplayers: ^6.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/
